---

title: 数据库：MySQL执行原理与架
date: 2026-06-17 08:00:00 +0800

categories: [数据? MySQL]

tags: [MySQL, 架构, 执行原理, 零基础]

math: true

mermaid: true

---



## 一?SQL 是如何执行的?


想象你走进一家餐厅点餐：



1. 你对服务员说"给我来一份宫保鸡?——这就是你的 SQL

2. 服务员确认你是本店会员，把菜单记下来——这是连接器和分析器

3. 厨师长看了一眼菜谱说"这道菜做过，直接?——这是查询缓?
4. 厨师长决定先炒鸡丁还是先备调料——这是优化器

5. 厨师按流程炒菜——这是执行器

6. 菜端上桌——结果返回给?


MySQL 的执行过程，和餐厅的运作一模一样?


```mermaid

flowchart LR

    A["客户?] --> B["连接?]

    B --> C["查询缓存"]

    C --> D["分析?]

    D --> E["优化?]

    E --> F["执行?]

    F --> G["存储引擎"]

    G --> H["磁盘"]



    style A fill:#4a90d9,color:#fff

    style G fill:#f5a623,color:#fff

    style H fill:#e74c3c,color:#fff

```



> 💡 **核心认知**：MySQL 分为 **Server ?*（连接器、缓存、分析器、优化器、执行器）和 **存储引擎?*（InnoDB、MyISAM 等）。Server 层做逻辑处理，存储引擎负责数据读写?
{: .prompt-tip }



---



## 连接器：先确认你是谁



当你执行 `mysql -u root -p` 时，第一步就?*建立连接**?


### 连接器做了什么？



1. **TCP 握手**：客户端?MySQL 建立网络连接

2. **身份认证**：验证用户名密码是否正确

3. **权限检?*：你这个账号能查哪些表、能做什么操?


```

客户?── 握手 ──?MySQL

               ├─ 用户名密码验??
               └─ 读取权限表，记住你是?
```



### 连接池：反复建立连接很浪?


每次建立连接都要?TCP 握手、身份验证、权限读取，开销很大。所以生产环境通常?*连接?*——提前建好一批连接，谁用就借给谁，用完还回来?


| 概念 | 类比 | 作用 |

|:---:|:---:|:---:|

| **长连?* | 包月会员，一直用 | 减少连接开销，适合频繁查询 |

| **短连?* | 临时顾客，用完就?| 适合偶尔用一?|

| **连接?* | 餐厅预留座位 | 控制并发，管理连接生命周?|



> ⚠️ **注意**：长连接虽然省资源，但长时间不关闭可能导?MySQL 内存占用越来越大（连接内存不会释放）。建议定期断开重连，或者用 `mysql_reset_connection` 重置连接状态?
{: .prompt-warning }



---



## 查询缓存：直接端上来



连接建立后，MySQL 收到你的 SQL，先做一件事—?*看看这道菜之前做过没**?


### 缓存如何工作?


```

SQL: SELECT * FROM users WHERE id = 1

        ?
    ?SQL 做哈希（计算一?指纹"?
        ?
    查缓存表：这个指纹有没有对应的结果？

        ├─ ???直接返回结果（超快！?
        └─ 没有 ??继续后面的流?
```



### 缓存的致命问题：太容易失效了



**只要表有任何改动（INSERT/UPDATE/DELETE），这张表的所有缓存全部失?*?


想象一下：你存?1000 条缓存记录，都关?`users` 表。现在有一个用户改了昵称—?000 条缓存瞬间全部作废?


| 场景 | 缓存是否好用?| 原因 |

|:---:|:---:|:---:|

| 静态数据（配置表、字典表?| ?非常好用 | 很少改动 |

| 读写均衡的业务表 | ?不要?| 写操作会频繁清空缓存 |

| 写入频繁的表 | ?绝对不用 | 缓存几乎永远是空?|



> 💡 **MySQL 8.0 已经移除了查询缓存功?*，因为实践证明弊大于利。现代应用通常?Redis 等外部缓存替代?
{: .prompt-tip }



---



## 分析器：你到底想做什?


缓存没命中，MySQL 开?*理解你的 SQL**?


### 第一步：词法分析



把你?SQL 拆成一个个"??


```

SELECT   name, age   FROM   users   WHERE   id = 1

   ?       ?         ?     ?       ?     ?
关键?  字段列表     关键?  表名    关键? 条件

```



就像你说"给我一份宫保鸡丁，不要?——服务员要拆?宫保鸡丁"是菜名，"不要?是特殊要求?


### 第二步：语法分析



按照 MySQL 的语法规则，把这些词组装成一?*语法?*?


```mermaid

graph TD

    A["SELECT语句"] --> B["字段列表"]

    A --> C["来源?]

    A --> D["过滤条件"]



    B --> B1["name"]

    B --> B2["age"]

    C --> C1["users"]

    D --> D1["id = 1"]



    style A fill:#4a90d9,color:#fff

```



### 第三步：语义分析



检查语法树合不合理?


- `users` 表存在吗?
- `name`、`age`、`id` 字段?`users` 表里吗？

- 你有权限查这些字段吗?


> 📖 **总结**：分析器 = 拆解 SQL + 检查语?+ 验证语义。如?SQL 写错了（比如?`SELECT` 拼写?`SELET`），就是在这一步报错?
{: .prompt-info }



---



## 优化器：怎么走最划算



分析器告?MySQL "用户想查什?，但**怎么查最?*——这是优化器的工作?


### 优化器在选择什么？



一?SQL 可能有很多种执行方式，优化器要选成本最低的?


| 决策类型 | 例子 |

|:---:|:---|

| **走哪个索?* | ?`WHERE name='小明' AND age=18`，是?name 索引还是 age 索引?|

| **表连接顺?* | 多表 JOIN 时，先查 A 表还是先?B 表？小表先查通常更快 |

| **全表扫还是走索引** | 如果要查的数据占了表的一半，全表扫描可能比索引还?|

| **排序方式** | `ORDER BY` 是走索引排序，还是临时文件排序？ |



### 理解"成本"



MySQL 的优化器基于**成本模型**（Cost Model）决策，主要考虑?


- **IO 成本**：从磁盘读数据页的开销（最昂贵?
- **CPU 成本**：在内存中比较、计算、排序的开销



```

总成??IO成本 × 权重 + CPU成本 × 权重

```



> 💡 **类比**：从北京去上海，有三种选择?br>?坐飞机（贵但?= 走索引）<br>?坐高铁（中等 = 覆盖索引?br>?走路去（免费但极?= 全表扫描?br><br>优化器会估算每种方式?成本"，选它认为最便宜的?
{: .prompt-tip }



### EXPLAIN：看优化器的决策



你可以用 `EXPLAIN` 查看优化器的选择?


```sql

EXPLAIN SELECT * FROM users WHERE id = 1;

```



关键信息?


| 字段 | 含义 | 关注?|

|:---:|:---:|:---:|

| `type` | 访问类型 | `system` > `const` > `eq_ref` > `ref` > `range` > `index` > `ALL`（越靠左越快?|

| `key` | 实际用的索引 | 是否用了你期望的索引?|

| `rows` | 预估扫描行数 | 越少越好 |

| `Extra` | 额外信息 | 是否出现?`Using filesort`、`Using temporary`?|



> ⚠️ **优化器不是神**。它基于统计信息估算，有时会选错（比如统计信息不准、索引选错）。这时候可以用 `FORCE INDEX` 强制指定索引?
{: .prompt-warning }



---



## 执行器：真正去执?


优化器确定了执行计划?*执行?*负责按照计划去取数据?


### 执行流程



?`SELECT name FROM users WHERE age > 18` 为例?


```

1. 检查权限：你能?users 表吗?
2. 打开表，调用存储引擎的接?
3. 取第一条数据：age > 18 吗？

   ├─ ??留下 name，放入结果集

   └─ ??跳过

4. 取下一条，重复步骤 3

5. 所有数据扫描完，返回结果集给客户端

```



### 执行器和存储引擎的配?


```mermaid

sequenceDiagram

    participant 执行?
    participant 存储引擎 as InnoDB



    执行?>>存储引擎: "给我 users 表第一条数?

    存储引擎->>执行? 返回数据?
    执行?>>执行? 检?age > 18?
    执行?>>存储引擎: "给我下一?

    存储引擎->>执行? 返回数据?
    执行?>>执行? 检?age > 18?
    Note right of 执行? 循环直到数据取完

```



> 📖 **核心设计**：执行器只关?取数据、判断、返??*数据具体怎么存、怎么?*交给存储引擎。这种分层设计让 MySQL 可以支持多种存储引擎（InnoDB、MyISAM、Memory 等）?
{: .prompt-info }



---



## 存储引擎：真正管数据的人



MySQL 最经典的设计就?*插件式存储引?*——Server 层负责逻辑，存储引擎负责数据的存和取?


### 三大存储引擎对比



| 特?| InnoDB（默认） | MyISAM | Memory |

|:---:|:---:|:---:|:---:|

| **事务** | ?支持 | ?不支?| ?不支?|

| **行锁** | ?支持 | ?只有表锁 | ?支持 |

| **崩溃恢复** | ?可以 | ?可能丢数?| ?重启就没?|

| **存储位置** | 磁盘 | 磁盘 | 内存 |

| **适用场景** | 大多数业务系?| 只读、日志类 | 临时表、缓?|



### InnoDB 为什么是默认?


```mermaid

flowchart TB

    A["InnoDB"] --> B["事务安全 ACID"]

    A --> C["行级?+ MVCC"]

    A --> D["B+树索?]

    A --> E["崩溃恢复能力"]

    A --> F["外键支持"]



    style A fill:#4a90d9,color:#fff

```



> 💡 **一句话总结**：MyISAM 跑得快但不安全（不支持事务、不支持行锁），InnoDB 是最均衡的选择——现?MySQL 应用 99% ?InnoDB?
{: .prompt-tip }



---



## 完整执行流程回顾



把一?`SELECT` 从头到尾再过一遍：



```mermaid

flowchart LR

    A["1. 建立连接<br/>TCP握手+认证"] --> B["2. 查询缓存<br/>命中则直接返?]

    B --> C["3. 分析?br/>词法+语法+语义分析"]

    C --> D["4. 优化?br/>选择执行计划"]

    D --> E["5. 执行?br/>调用存储引擎取数?]

    E --> F["6. 存储引擎<br/>从磁?Buffer Pool取数?]

    F --> G["7. 返回结果<br/>给客户端"]



    style A fill:#4a90d9,color:#fff

    style G fill:#f5a623,color:#fff

```



### 更新语句的特殊之?


如果?`UPDATE/INSERT/DELETE`，多了两个关键步骤：



```

执行器修改数?
        ?
    ?undo log（回滚日志——万一出错可以回滚?
        ?
    更新内存中的数据?
        ?
    ?redo log（重做日志——崩溃后可以恢复?
        ?
    ?binlog（归档日志——主从同步用?
        ?
    后台刷盘（把内存中的改动写回磁盘?
```



> 📖 **WAL（Write-Ahead Logging?*：先写日志，再写数据。这是数据库的核心设计—?*任何修改先记日志，再应用到数?*。好处是：即使数据库突然挂了，重启后可以根据日志恢复?
{: .prompt-info }



---



## 小结



MySQL 的架构设计体现了计算机系统最经典的思想—?*分层 + 各司其职**?


- **连接?*：认?+ 权限 + 连接管理

- **查询缓存**：快道（但弊大于利，MySQL 8.0 已移除）

- **分析?*：理?SQL 说什?
- **优化?*：决定怎么走最?
- **执行?*：按照计划执?
- **存储引擎**：真正读写数据（InnoDB 是默认）



理解这条执行链路，你就不?只会?SQL"的初级开发者了——你能看?EXPLAIN、理解索引为什么快、诊断查询慢的原因?
