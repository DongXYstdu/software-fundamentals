---
title: TCP 半连接队列和全连接队?
date: 2026-06-10 09:00:00 +0800
categories: [计算机网? TCP]
tags: [计算机网? TCP, 小林coding, 图解]
---

网上许多博客针对增大 TCP 半连接队列和全连接队列的方式如下?

- 增大 TCP 半连接队列的方式是增?/proc/sys/net/ipv4/tcp_max_syn_backlog?

- 增大 TCP 全连接队列的方式是增?listen() 函数中的 backlog?

这里先跟大家说下?*上面的方式都是不准确的?*

“你怎么知道不准确？?

很简单呀，因为我做了实验和看?TCP 协议栈的内核源码，发现要增大这两个队列长度，不是简简单单增大某一个参数就可以的?

接下来，就会?*实战 + 源码分析，带大家解密 TCP 半连接队列和全连接队列?*

“源码分析，那不是劝退吗？我们?Java 的看不懂呀?

放心，本文的源码分析不会涉及很深的知识，因为都被我删减了，你只需要会条件判断语句 if、左移右移操作符、加减法等基本语法，就可以看懂?

另外，不仅有源码分析，还会介?Linux 排查半连接队列和全连接队列的命令?

“哦？似乎很有看头，那我姑且看一下吧！?

行，没有被劝退的小伙伴，值得鼓励，下面这图是本文的提纲：

![](https://aka.doubaocdn.com/s/OXMk1wZOqR)

## 什么是 TCP 半连接队列和全连接队列？

?TCP 三次握手的时候，Linux 内核会维护两个队列，分别是：

- 半连接队列，也称 SYN 队列?

- 全连接队列，也称 accept 队列?

服务端收到客户端发起?SYN 请求后，**内核会把该连接存储到半连接队?* ，并向客户端响应 SYN+ACK，接着客户端会返回 ACK，服务端收到第三次握手的 ACK 后，**内核会把连接从半连接队列移除，然后创建新的完全的连接，并将其添加?accept 队列，等待进程调?accept 函数时把连接取出来?*

![](https://aka.doubaocdn.com/s/zqEs1wZOqR)

不管是半连接队列还是全连接队列，都有最大长度限制，超过限制时，内核会直接丢弃，或返?RST 包?

## 实战 - TCP 全连接队列溢?

如何知道应用程序?TCP 全连接队列大小？

在服务端可以使用 `ss` 命令，来查看 TCP 全连接队列的情况?

但需要注意的?`ss` 命令获取?`Recv-Q/Send-Q` 在「LISTEN 状态」和「非 LISTEN 状态」所表达的含义是不同的。从下面的内核代码可以看出区别：

![](https://aka.doubaocdn.com/s/wC2h1wZOqR)

在「LISTEN 状态」时，`Recv-Q/Send-Q` 表示的含义如下：

![](https://aka.doubaocdn.com/s/QO2V1wZOqR)

- Recv-Q：当前全连接队列的大小，也就是当前已完成三次握手并等待服务端 `accept()` ?TCP 连接?

- Send-Q：当前全连接最大队列长度，上面的输出结果说明监?8088 端口?TCP 服务，最大全连接长度?128?

在「非 LISTEN 状态」时，`Recv-Q/Send-Q` 表示的含义如下：

![](https://aka.doubaocdn.com/s/oRY21wZOqR)

- Recv-Q：已收到但未被应用进程读取的字节数；

- Send-Q：已发送但未收到确认的字节数；

如何模拟 TCP 全连接队列溢出的场景?

实验环境?

- 客户端和服务端都?CentOs 6.5 ，Linux 内核版本 2.6.32

- 服务?IP 192.168.3.200，客户端 IP 192.168.3.100

- 服务端是 Nginx 服务，端口为 8088

这里先介绍下 `wrk` 工具，它是一款简单的 HTTP 压测工具，它能够在单机多?CPU 的条件下，使用系统自带的高性能 I/O 机制，通过多线程和事件模式，对目标机器产生大量的负载?

本次模拟实验就使?`wrk` 工具来压力测试服务端，发起大量的请求，一起看看服务端 TCP 全连接队列满了会发生什么？有什么观察指标？

客户端执?`wrk` 命令对服务端发起压力测试，并?3 万个连接?

在服务端可以使用 `ss` 命令，来查看当前 TCP 全连接队列的情况?

![](https://aka.doubaocdn.com/s/xhNy1wZOqR)

其间共执行了两次 ss 命令，从上面的输出结果，可以发现当前 TCP 全连接队列上升到?129 大小，超过了最?TCP 全连接队列?

**当超过了 TCP 最大全连接队列，服务端则会丢掉后续进来?TCP 连接** ，丢掉的 TCP 连接的个数会被统计起来，我们可以使用 netstat -s 命令来查看：

上面看到?41150 times ，表示全连接队列溢出的次数，注意这个是累计值。可以隔几秒钟执行下，如果这个数字一直在增加的话肯定全连接队列偶尔满了?

从上面的模拟结果，可以得知，**当服务端并发处理大量请求时，如果 TCP 全连接队列过小，就容易溢出。发?TCP 全连接队溢出的时候，后续的请求就会被丢弃，这样就会出现服务端请求数量上不去的现象?*

![](https://aka.doubaocdn.com/s/8XpM1wZOqR)

Linux 有个参数可以指定?TCP 全连接队列满了会使用什么策略来回应客户端?

实际上，丢弃连接只是 Linux 的默认行为，我们还可以选择向客户端发?RST 复位报文，告诉客户端连接已经建立失败?

![](https://aka.doubaocdn.com/s/8lU61wZOqR)

tcp_abort_on_overflow 共有两个值分别是 0 ?1，其分别表示?

- 0 ：如果全连接队列满了，那?server 扔掉 client 发过来的 ack ?

- 1 ：如果全连接队列满了，server 发送一?`reset` 包给 client，表示废掉这个握手过程和这个连接?

如果要想知道客户端连接不上服务端，是不是服务?TCP 全连接队列满的原因，那么可以?tcp_abort_on_overflow 设置?1，这时如果在客户端异常中可以看到很多 `connection reset by peer` 的错误，那么就可以证明是由于服务?TCP 全连接队列溢出的问题?

通常情况下，应当?tcp_abort_on_overflow 设置?0，因为这样更有利于应对突发流量?

举个例子，当 TCP 全连接队列满导致服务器丢掉了 ACK，与此同时，客户端的连接状态却?ESTABLISHED，进程就在建立好的连接上发送请求。只要服务器没有为请求回?ACK，请求就会被多次**重发** 。如果服务器上的进程只是**短暂的繁忙造成 accept 队列满，那么?TCP 全连接队列有空位时，再次接收到的请求报文由于含有 ACK，仍然会触发服务器端成功建立连接?*

所以，tcp_abort_on_overflow 设为 0 可以提高连接建立的成功率，只有你非常肯定 TCP 全连接队列会长期溢出时，才能设置?1 以尽快通知客户端?

如何增大 TCP 全连接队列呢?

是的，当发现 TCP 全连接队列发生溢出的时候，我们就需要增大该队列的大小，以便可以应对客户端大量的请求?

**TCP 全连接队列的最大值取决于 somaxconn ?backlog 之间的最小值，也就?min(somaxconn, backlog)** 。从下面?Linux 内核代码可以得知?

- `somaxconn` ?Linux 内核的参数，默认值是 128，可以通过 `/proc/sys/net/core/somaxconn` 来设置其值；

- `backlog` ?`listen(int sockfd, int backlog)` 函数中的 backlog 大小，Nginx 默认值是 511，可以通过修改配置文件设置其长度；

前面模拟测试中，我的测试环境?

- somaxconn 是默认?128?

- Nginx ?backlog 是默认?511

所以测试环境的 TCP 全连接队列最大值为 min(128, 511)，也就是 `128`，可以执?`ss` 命令查看?

![](https://aka.doubaocdn.com/s/VOpc1wZOqR)

现在我们重新压测，把 TCP 全连接队?*搞大** ，把 `somaxconn` 设置?5000?

接着?Nginx ?backlog 也同样设置成 5000?

![](https://aka.doubaocdn.com/s/zT1N1wZOqR)

最后要重启 Nginx 服务，因为只有重新调?`listen()` 函数 TCP 全连接队列才会重新初始化?

重启完后 Nginx 服务后，服务端执?ss 命令，查?TCP 全连接队列大小：

![](https://aka.doubaocdn.com/s/40xk1wZOqR)

从执行结果，可以发现 TCP 全连接最大值为 5000?

增大 TCP 全连接队列后，继续压?

客户端同样以 3 万个连接并发发送请求给服务端：

服务端执?`ss` 命令，查?TCP 全连接队列使用情况：

![](https://aka.doubaocdn.com/s/3Mmd1wZOqR)

从上面的执行结果，可以发现全连接队列使用增长的很快，但是一直都没有超过最大值，所以就不会溢出，那?`netstat -s` 就不会有 TCP 全连接队列溢出个数的显示?

说明 TCP 全连接队列最大值从 128 增大?5000 后，服务端抗住了 3 万连接并发请求，也没有发生全连接队列溢出的现象了?

**如果持续不断地有连接因为 TCP 全连接队列溢出被丢弃，就应该调大 backlog 以及 somaxconn 参数?*

## 实战 - TCP 半连接队列溢?

如何查看 TCP 半连接队列长度？

很遗憾，TCP 半连接队列长度的长度，没有像全连接队列那样可以用 ss 命令查看?

但是我们可以抓住 TCP 半连接的特点，就是服务端处于 `SYN_RECV` 状态的 TCP 连接，就?TCP 半连接队列?

于是，我们可以使用如下命令计算当?TCP 半连接队列长度：

![](https://aka.doubaocdn.com/s/qAyA1wZOqR)

如何模拟 TCP 半连接队列溢出场景？

模拟 TCP 半连接溢出场景不难，实际上就是对服务端一直发?TCP SYN 包，但是不回第三次握?ACK，这样就会使得服务端有大量的处于 `SYN_RECV` 状态的 TCP 连接?

这其实也就是所谓的 SYN 洪泛、SYN 攻击、DDos 攻击?

实验环境?

- 客户端和服务端都?CentOs 6.5 ，Linux 内核版本 2.6.32

- 服务?IP 192.168.3.200，客户端 IP 192.168.3.100

- 服务端是 Nginx 服务，端口为 8088

注意：本次模拟实验是没有开?tcp_syncookies，关?tcp_syncookies 的作用，后续会说明?

本次实验使用 `hping3` 工具模拟 SYN 攻击?

![](https://aka.doubaocdn.com/s/BNnA1wZOqR)

当服务端受到 SYN 攻击后，连接服务?ssh 就会断开了，无法再连上。只能在服务端主机上执行查看当前 TCP 半连接队列大小：

同时，还可以通过 netstat -s 观察半连接队列溢出的情况?

上面输出的数值是**累计?* ，表示共有多少个 TCP 连接因为半连接队列溢出而被丢弃?*隔几秒执行几次，如果有上升的趋势，说明当前存在半连接队列溢出的现?* ?

大部分人都说 tcp_max_syn_backlog 是指定半连接队列的大小，是真的吗?

很遗憾，半连接队列的大小并不单单只跟 `tcp_max_syn_backlog` 有关系?

上面模拟 SYN 攻击场景时，服务端的 tcp_max_syn_backlog 的默认值如下：

但是在测试的时候发现，服务端最多只?256 个半连接队列，而不?512，所?*半连接队列的最大长度不一定由 tcp_max_syn_backlog 值决定的** ?

接下来，走进 Linux 内核的源码，来分?TCP 半连接队列的最大值是如何决定的?

TCP 第一次握手（收到 SYN 包）?Linux 内核代码如下，其中缩减了大量的代码，只需要重点关?TCP 半连接队列溢出的处理逻辑?

---

> 参考来源：[TCP 半连接队列和全连接队列](https://xiaolincoding.com/network/3_tcp/tcp_queue.html)
