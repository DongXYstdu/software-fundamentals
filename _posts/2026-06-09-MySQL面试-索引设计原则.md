---
title: 设计索引时应遵循哪些原则�?
date: 2026-06-09 09:00:00 +0800
categories: [数据�? MySQL]
tags: [MySQL, 面试, 小哈学Java]
---

## 面试考察�?

1. **实战经验**：面试官不仅仅想知道你会不会�?`CREATE INDEX`，更想考察你是否具备在生产环境中设计合理索引的能力，能否平衡查询性能与写入成本�?

2. **原理理解**：是否理�?B+ 树结构、最左前缀原则、索引下推等底层原理，而非只会死记硬背几条规则�?

3. **权衡意识**：索引不是越多越好，考察你是否懂得在查询性能、存储空间、写入性能之间做取舍�?

## 核心答案

设计索引应遵循以�?**7 大核心原�?*�?

| 原则 | 核心要点 | 关键�?|
|---|---|---|
| 选择合适的�?| WHERE、JOIN、ORDER BY、GROUP BY 列优�?| 高频查询�?|
| 区分度高优先 | 选择�?= 唯一�?总行数，越高越好 | 基数大、离散度�?|
| 最左前缀原则 | 联合索引按顺序匹配，从左到右连续使用 | 范围查询断后 |
| 覆盖索引 | 索引包含查询所需全部字段，避免回�?| `SELECT` 列也在索引中 |
| 控制索引数量 | 单表索引不超�?5 个，单个索引字段不超�?5 �?| 写入成本、空间占�?|
| 前缀索引 | 长字符串只索引前 N 个字符，节省空间 | 牺牲部分选择�?|
| 索引下推 | MySQL 5.6+ 特性，在索引层过滤减少回表 | `Using index condition` |

**一句话总结**：索引设计要 "选对列、区分度高、遵循最左前缀、善用覆盖索引、控制数�?�?

## 深度解析

### 一、选择合适的列建立索�?

![](https://aka.doubaocdn.com/s/3dBG1wZNod)

**�?适合建索引的场景**�?

- **WHERE 条件�?*：查询条件中频繁出现的列，能大幅减少扫描行数�?
- **JOIN 关联�?*：外键关联列，能加速多表关联查询�?
- **ORDER BY / GROUP BY �?*：排序和分组操作非常消耗性能，索引可以利�?B+ 树的有序性直接获取结果�?
- **高基数列**：唯一值越多，索引的过滤效果越好�?

**�?不适合建索引的场景**�?

- **区分度低的列**：如性别（只�?"�?�?）、状态（只有 "启用/禁用"），索引过滤效果差，优化器可能直接选择全表扫描�?
- **频繁更新的列**：每次更新都会导致索引重建，增加写入成本�?
- **小表**：数据量小于 1000 行时，全表扫描可能比走索引更快�?

### 二、区分度（选择性）原则

**区分�?= 唯一值数�?/ 总行�?*，值越接近 1 越好�?

```sql
-- 查看列的区分�?
SELECT
    COUNT(DISTINCT user_id) / COUNT(*) AS user_id_selectivity,
    COUNT(DISTINCT status) / COUNT(*) AS status_selectivity
FROM orders;

-- 结果示例�?
-- user_id_selectivity: 0.85 �?区分度高，适合建索�?
-- status_selectivity: 0.01 �?区分度低，不适合单独建索�?
```

**联合索引的列顺序**：区分度高的列放前面�?

```sql
-- 假设 status 只有 3 个值，user_id �?10 万个�?
-- �?错误：区分度低的列在�?
CREATE INDEX idx_wrong ON orders(status, user_id);

-- �?正确：区分度高的列在�?
CREATE INDEX idx_right ON orders(user_id, status);
```

### 三、最左前缀原则

联合索引按照定义顺序构建 B+ 树，查询时必须从最左列开始连续匹配�?

![](https://aka.doubaocdn.com/s/z5mO1wZNod)

- **从左到右连续匹配**：必须从索引的第一列开始，中间不能跳过任何一列�?
- **范围查询�?断后"**：一旦出现范围查询（`>`、`<`、`BETWEEN`、`LIKE` 前缀模糊），该列之后的索引列无法被使用�?
- **优化建议**：把范围查询列放在联合索引的最后�?

```sql
-- 场景：经常查询某用户的已支付订单
-- �?推荐：等值条件在前，范围条件在后
CREATE INDEX idx_user_status_time ON orders(user_id, status, create_time);

-- 查询能完整利用索�?
SELECT * FROM orders
WHERE user_id = 100 AND status = 'PAID' AND create_time > '2024-01-01';
```

### 四、覆盖索�?

如果索引包含了查询所需的所有字段，MySQL 可以直接从索引中获取数据�?*无需回表**查询聚簇索引�?

```sql
-- 假设有索引：INDEX(user_id, status)
-- �?需要回表：SELECT 中有索引不包含的字段
SELECT user_id, status, amount FROM orders WHERE user_id = 100;

-- �?覆盖索引：SELECT 字段都在索引�?
SELECT user_id, status FROM orders WHERE user_id = 100;

-- �?更好的方案：创建包含 amount 的联合索�?
CREATE INDEX idx_covering ON orders(user_id, status, amount);
```

**EXPLAIN 中显�?`Using index` 表示使用了覆盖索�?*�?

### 五、控制索引数�?

索引不是越多越好，每个索引都有成本：

![](https://aka.doubaocdn.com/s/h2Mx1wZNod)

- **空间成本**：每个索引都是一棵独立的 B+ 树，会占用额外的磁盘空间。联合索引字段越多，空间占用越大�?

- **时间成本（写入时�?*�?
  - `INSERT` 操作需要同时更新所有索�?
  - `UPDATE` 操作如果修改了索引列，需要先删除旧的索引项，再插入新的索引项
  - `DELETE` 操作需要从所有索引中删除对应的数�?

- **优化器成�?*：索引越多，MySQL 优化器在生成执行计划时需要考虑的选项越多，可能导致选择时间变长，甚至选错索引�?

**经验法则**�?

- 单表索引数量�?*不超�?5 �?*
- 单个索引字段数：**不超�?5 �?*
- 定期审查：删�?unused 索引

```sql
-- 查看索引使用情况（MySQL 5.7+�?
SELECT * FROM sys.schema_unused_indexes
WHERE object_schema = 'your_database';
```

### 六、前缀索引

对于 `VARCHAR`、`TEXT` 等长字符串列，可以使用前缀索引只索引前 N 个字符，节省空间�?

```sql
-- 创建前缀索引，只索引 email �?10 个字�?
CREATE INDEX idx_email_prefix ON users(email(10));

-- 查询时正常使�?
SELECT * FROM users WHERE email = 'test@example.com';
```

**权衡**�?

- �?节省索引空间
- �?可能降低区分度（�?N 个字符相同的情况�?
- �?无法用于覆盖索引（必须回表验证完整值）

**如何确定前缀长度**�?

```sql
-- 计算不同前缀长度的区分度
SELECT
    COUNT(DISTINCT LEFT(email, 5)) / COUNT(*) AS prefix_5,
    COUNT(DISTINCT LEFT(email, 10)) / COUNT(*) AS prefix_10,
    COUNT(DISTINCT LEFT(email, 15)) / COUNT(*) AS prefix_15,
    COUNT(DISTINCT email) / COUNT(*) AS full_column
FROM users;

-- 选择区分度接近完整列的最短前缀
```

### 七、索引下推（Index Condition Pushdown，ICP�?

MySQL 5.6+ 引入的优化特性，将部�?WHERE 条件的过滤下推到存储引擎层，减少回表次数�?

```sql
-- 假设有索引：INDEX(name, age)
-- �?ICP：存储引擎返回所�?name 匹配的记录，Server 层再过滤 age
-- �?ICP：存储引擎直接过�?name AND age，只返回符合条件的记�?
SELECT * FROM users WHERE name LIKE '�?' AND age = 25;
```

**EXPLAIN 中显�?`Using index condition` 表示使用了索引下�?*�?

## 面试高频追问

1. **联合索引的列顺序如何确定�?*

   区分度高的列放前面、等值条件列放前面、范围查询列放后面、考虑排序分组需求�?

2. **什么情况下索引会失效？**

   对索引列使用函数或计算、隐式类型转换、`LIKE` �?`%` 开头、`OR` 条件中有列无索引、联合索引不满足最左前缀、否定条件（`!=`、`NOT IN`）等�?

3. **主键为什么建议使用自�?ID�?*

   自增 ID 保证新数据顺序插�?B+ 树末尾，避免页分裂，减少碎片，提高写入性能和空间利用率�?

## 常见面试变体

- "联合索引 (a, b, c)，WHERE b = 1 AND c = 2 能走索引吗？"
- "什么情况下应该使用前缀索引�?
- "索引越多越好吗？为什么？"
- "覆盖索引是什么？有什么优势？"

## 记忆口诀

**索引设计七原�?*：选对列、区分高、最左前、覆盖好、控数量、前缀省、下推快

**联合索引顺序**：区分度高放前头，等值条件优先走，范围查询放最�?

## 总结

索引设计需要在查询性能和写入成本之间做权衡。核心原则是：选择高频查询且区分度高的列建立索引；联合索引遵循最左前缀原则，区分度高的列放前面；善用覆盖索引避免回表；控制索引数量避免过度索引；长字符串可使用前缀索引节省空间。记住，索引不是越多越好，要根据实际业务场景和查询模式进行设计�?

---
> 参考来源：[MySQL 设计索引时应遵循哪些原则？](https://www.quanxiaoha.com/java-interview/mysql-index-design-principles)
