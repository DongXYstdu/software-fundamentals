---
title: 唯一索引和主键索引的区别
date: 2026-06-09 09:00:00 +0800
categories: [数据? MySQL]
tags: [MySQL, 面试, 小哈学Java]
---

## 面试考察?

1. **索引类型理解**：面试官不仅仅是想知?"有什么区?，更是想考察你是否理解主键索引（聚簇索引）和唯一索引（二级索引）在存储结构上的根本差异?

2. **NULL 值处?*：考察你是否清楚主键不允许 NULL，而唯一索引可以（但只能有一个），这是很多面试者容易忽略的细节?

3. **性能优化意识**：理解聚簇索引和非聚簇索引的查询效率差异，能否在设计表结构时做出正确选择?

## 核心答案

**主键索引**：一种特殊的唯一索引，每张表只能有一个，用于唯一标识每一行记录，InnoDB 中主键是**聚簇索引**?

**唯一索引**：保证索引列的值唯一，一张表可以有多个唯一索引，InnoDB 中唯一索引?*二级索引（非聚簇索引?*?

**核心区别对比**?

| 对比维度 | 主键索引（PRIMARY?| 唯一索引（UNIQUE?|
|---|---|---|
| 数量限制 | 每表**只能?1 ?* | 每表**可以有多?* |
| NULL ?| **不允?* NULL | **允许** NULL（但最?1 个） |
| 存储结构 | 聚簇索引，叶子节点存完整数据 | 二级索引，叶子节点存主键?|
| 索引回表 | 不需要回?| 需要回表（查询非索引列时） |
| 创建语法 | `PRIMARY KEY (col)` | `UNIQUE KEY uk_name (col)` |
| 是否必须 | 建议有，但不强制 | 按需创建 |

**一句话总结**：主键是**唯一的聚簇索?*，不允许 NULL；唯一索引?*可以有多个的非聚簇索?*，允?NULL?

## 深度解析

### 一、存储结构差异：聚簇索引 vs 二级索引

主键索引和唯一索引?InnoDB 中的存储结构完全不同?

![](https://aka.doubaocdn.com/s/QcBq1wZNov)

上图对比了主键索引和唯一索引的存储结构。关键区别在于：

- **主键索引（聚簇索引）**：叶子节点直接存?*完整的行数据**，通过主键查询可以直接获取所有字段，无需回表
- **唯一索引（二级索引）**：叶子节点只存储**索引列的?+ 主键?*，如果查询其他字段，需要拿着主键值回表查询聚簇索?

### 二、NULL 值处理差?

这是面试中的高频考点?

```sql
-- 创建测试?
CREATE TABLE user (
    id BIGINT PRIMARY KEY,           -- 主键，不允许 NULL
    email VARCHAR(50) UNIQUE,        -- 唯一索引，允?NULL
    phone VARCHAR(20) UNIQUE         -- 唯一索引，允?NULL
);

-- 主键测试：插?NULL 会报?
INSERT INTO user (id, email) VALUES (NULL, 'test@qq.com');
-- ?ERROR 1048: Column 'id' cannot be null

-- 唯一索引测试：允?NULL，且 MySQL 中可以插入多?NULL
INSERT INTO user (id, email) VALUES (1, NULL);      -- ?成功
INSERT INTO user (id, email) VALUES (2, NULL);      -- ?成功（MySQL 认为多个 NULL 不重复）
INSERT INTO user (id, email) VALUES (3, 'a@qq.com'); -- ?成功
INSERT INTO user (id, email) VALUES (4, 'a@qq.com'); -- ?Duplicate entry
```

![](https://aka.doubaocdn.com/s/anQN1wZNov)

**关键结论**?

- **主键**：绝对不允许 NULL，这是主键的基本约束
- **唯一索引**：允?NULL，且 MySQL 中可以插入多?NULL 值（因为 `NULL != NULL`?

### 三、查询性能差异：回表问?

通过主键查询 vs 通过唯一索引查询的性能差异?

```sql
-- 表结?
CREATE TABLE user (
    id BIGINT PRIMARY KEY,
    email VARCHAR(50) UNIQUE,
    name VARCHAR(50),
    age INT
);

-- 场景 1：通过主键查询
SELECT * FROM user WHERE id = 1;
-- ?直接走聚簇索引，一次查询即可获取完整数?

-- 场景 2：通过唯一索引查询所有字?
SELECT * FROM user WHERE email = 'test@qq.com';
-- ⚠️ 需要回表：先查唯一索引得到 id，再回表查聚簇索?

-- 场景 3：通过唯一索引查询索引列（覆盖索引?
SELECT email FROM user WHERE email = 'test@qq.com';
-- ?覆盖索引，不需要回?
```

![](https://aka.doubaocdn.com/s/Oaxi1wZNov)

### 四、使用场景建?

| 场景 | 推荐索引类型 | 原因 |
|---|---|---|
| 标识每一行记?| 主键索引 | 每表必须有唯一标识，推荐自?BIGINT |
| 用户邮箱不能重复 | 唯一索引 | 业务唯一性约束，允许未设置邮箱（NULL?|
| 手机号唯一 | 唯一索引 | 允许用户暂未绑定手机?|
| 身份证号唯一 | 唯一索引 | 允许 NULL（可能未录入?|
| 联合唯一（用?+ 日期?| 联合唯一索引 | `UNIQUE KEY uk_user_date (user_id, date)` |

**最佳实?*?

```sql
-- 推荐：使?BIGINT 自增主键
CREATE TABLE orders (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    order_no VARCHAR(32) UNIQUE NOT NULL,  -- 业务订单号，不允?NULL
    user_id BIGINT NOT NULL,
    INDEX idx_user (user_id)
);

-- 不推荐：没有主键的表
-- MySQL 会自动选择一个非空唯一索引作为聚簇索引
-- 如果没有合适的，会生成一个隐藏的 6 字节主键
```

### 五、如果没有主键会怎样?

InnoDB 要求每张表必须有聚簇索引?

![](https://aka.doubaocdn.com/s/pVvq1wZNov)

InnoDB 选择聚簇索引的优先级?

1. 显式定义的主?
2. 第一个非空唯一索引
3. 自动生成 6 字节隐藏主键 `ROW_ID`

## 面试高频追问

1. **追问一**：为什么推荐使用自增主键而不?UUID?
    - 答：自增主键是顺序插入的，B+ 树叶子节点顺序追加，不会产生页分裂；UUID 是无序的，插入会导致频繁页分裂，影响性能。此外，UUID 占用空间大（36 字符 vs 8 字节），降低索引效率?

2. **追问?*：一张表可以没有主键吗？
    - 答：可以，但 InnoDB 会自动选择一个非空唯一索引作为聚簇索引；如果没有合适的，会生成隐藏?6 字节 `ROW_ID`。但强烈不建议这样做，应该显式定义主键?

3. **追问?*：联合主键和联合唯一索引有什么区别？
    - 答：本质区别和单列一样——联合主键是聚簇索引，不允许任何列为 NULL；联合唯一索引是二级索引，允许列为 NULL?

## 常见面试变体

- "主键索引和唯一索引有什么区别？"
- "聚簇索引和非聚簇索引的区别是什么？"
- "MySQL 查询走唯一索引时为什么可能需要回表？"
- "唯一索引允许 NULL 吗？主键呢？"

## 记忆口诀

**主键 vs 唯一索引**?

1. **主键聚簇**：叶子存完整数据，查询不回表
2. **唯一二级**：叶子存主键值，查询需回表
3. **NULL 有别**：主键不允许，唯一索引可以
4. **数量不同**：主键唯一一个，唯一索引多个

## 总结

主键索引?*聚簇索引**，每表只能有一个，不允?NULL，叶子节点存储完整行数据；唯一索引?*二级索引**，可以有多个，允?NULL（可多个），叶子节点存储主键值。查询时主键直接获取数据，唯一索引需要回表。推荐使?`BIGINT` 自增主键，业务唯一约束用唯一索引?

---
> 参考来源：[MySQL 唯一索引和主键索引的区别？](https://www.quanxiaoha.com/java-interview/mysql-unique-index-vs-primary-key-difference)
