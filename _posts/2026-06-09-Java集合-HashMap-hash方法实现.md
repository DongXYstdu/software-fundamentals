---
title: HashMap hash 方法是如何实现的
date: 2026-06-09 09:00:00 +0800
categories: [Java, 集合]
tags: [Java, 集合, 面试, 小哈学Java]
---

## 面试考察?

1. **源码掌握**：面试官不仅仅是想知?"有个 hash 方法" 这个事实，更是想知道你是否读过源码，是否理解 `(h = key.hashCode()) ^ (h >>> 16)` 这行代码的精妙之处?

2. **位运算理?*：考察你是否理解无符号右移 `>>>` 和异?`^` 的作用，以及为什么这样设计能减少哈希冲突?

3. **设计思想**：是否理?"扰动函数" 的概念，以及高位参与运算对散列均匀性的影响?

## 核心答案

HashMap ?`hash()` 方法通过 **"扰动函数"** ?key ?`hashCode()` ?16 位与?16 位异或，让高位信息参与散列运算：

```java
static final int hash(Object key) {
    int h;
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);
}
```

| 关键?| 说明 |
|---|---|
| **null 处理** | key ?null 时返?0（HashMap 允许 null key?|
| **扰动算法** | `hashCode ^ (hashCode >>> 16)` |
| **目的** | 让高位参与运算，减少哈希冲突 |
| **效果** | 散列更均匀，冲突概率降?|

**一句话总结**：hash 方法通过 **?16 位异或低 16 ?* 的扰动算法，让散列值更均匀，减少冲突?

## 深度解析

### 一、源码逐行解析

```java
static final int hash(Object key) {
    int h;
    // 1. key ?null，返?0（HashMap 支持 null key?
    // 2. key 不为 null，计?hashCode 并扰?
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);
}
```

逐行解析?

- **`key == null ? 0`**：HashMap 允许一?null 键，null ?hash 值固定为 0，所?null 键总是放在?0 的位置?
- **`h = key.hashCode()`**：先调用 key 对象?`hashCode()` 方法获取原始哈希值?
- **`h >>> 16`**：将 hashCode 无符号右?16 位，取出?16 位?
- **`^` 异或操作**：将?16 位与?hashCode 异或，得到扰动后的哈希值?

### 二、为什么要扰动?

![](https://aka.doubaocdn.com/s/Bxh91wZNnl)

上图解释了扰动函数的核心价值。关键理解：

- **问题根源**：桶索引只用?hashCode 的低位，高位信息被浪?
- **扰动作用**：将高位信息 "混合" 到低位，让低位也包含高位的特?
- **效果**：原本高位不同但低位相同?key，扰动后低位也变得不同，减少冲突

### 三、扰动过程图?

![](https://aka.doubaocdn.com/s/FVtJ1wZNnl)

上图详细展示了扰动函数的执行过程。关键点?

- **异或特?*：相同为 0，不同为 1。高 16 位与自己异或保持不变，低 16 位与?16 位异或产生变化?
- **只扰动一?*：为什么不是多次扰动？一次扰动已经足够，多次扰动反而可能增加计算开销?
- **?16 位变?*：低 16 位融合了?16 位的信息，即使两?hashCode 低位相同，只要高位不同，扰动后低位也会不同?

### 四、为什么用异或而不是与/或？

![](https://aka.doubaocdn.com/s/VKtm1wZNnl)

上图解释了为什么选择异或运算。核心原因：

- **与运?*：偏?0，散列不均匀
- **或运?*：偏?1，散列不均匀
- **异或运算**? ?1 概率相等，散列最均匀

### 五、与桶定位的关系

![](https://aka.doubaocdn.com/s/Wgzi1wZNnl)

## 面试高频追问

1. **为什么只右移 16 位？不是 8 位或 24 位？**
    - 32 位整数，右移 16 位正好是高低位对?
    - 既能利用高位信息，又不会过度破坏低位原始特征
    - 实践证明效果最?

2. **JDK 1.7 ?1.8 ?hash 方法有区别吗?*
    - 有！JDK 1.7 扰动?4 次（多次移位异或?
    - JDK 1.8 简化为 1 次，性能更好，效果相?

3. **扰动函数能完全避免冲突吗?*
    - 不能！只能减少冲突概?
    - 完全避免冲突需要完美哈希函数，实际不存?

4. **为什么用 `>>>` 无符号右移而不?`>>` 有符号右移？**
    - `>>>` 高位?0，`>>` 高位补符号位
    - hashCode 可能为负数，?`>>` 会引入符号位干扰
    - ?`>>>` 保证高位始终?0，扰动更纯净

## 常见面试变体

- "HashMap 如何计算 key 的哈希值？"
- "为什?HashMap 要对 hashCode 进行扰动?
- "HashMap ?hash 方法为什么用异或?

## 记忆口诀

**扰动公式**：`hash = hashCode ^ (hashCode >>> 16)`

**作用**：高位异或低位，让高位信息参与散列，减少冲突?

**null 处理**：key ?null 返回 0，统一放桶 0?

## 总结

HashMap ?`hash()` 方法通过 **`hashCode ^ (hashCode >>> 16)`** 扰动算法，将?16 位与?16 位异或，让高位信息参与散列运算。这是因为桶索引只用到低位，扰动可以让高位原本不同但低位相同?key 散列到不同桶，减少冲突。选择异或是因为它产生 0 ?1 的概率相等，散列最均匀。null 键的 hash 固定?0?

---
> 参考来源：[HashMap ?hash 方法是如何实现的？](https://www.quanxiaoha.com/java-interview/how-hashmap-hash-method-works)
