---
title: 脏读、幻读、不可重复读是什么？
date: 2026-06-09 09:00:00 +0800
categories: [数据? MySQL]
tags: [MySQL, 面试, 小哈学Java]
---

## 面试考察?

1. **对数据库事务核心特性的理解**：事务的 ACID 特性是基础，而隔离性（Isolation）直接衍生出这些问题?

2. **对并发事务可能引发问题的洞察?*：不仅仅是背诵定义，更是想知道你是否?*理解**在多个事务并发执行时，数据一致性可能受到何种挑战?

3. **?MySQL 隔离级别机制的掌?*：考察你是否清楚，数据库通过设置不同?*隔离级别**（Read Uncommitted, Read Committed, Repeatable Read, Serializable）来 trade-off 性能与一致性，从而解决这些现象?

4. **解决实际问题的能?*：在真实的高并发业务场景（如金融交易、库存扣减）中，你是否能识别并选择正确的隔离级别或加锁策略来规避这些问题?

## 核心答案

脏读、幻读和不可重复读是数据库事?*隔离?*被破坏时，可能出现的三种典型问题。它们都源于**并发事务**之间的相互干扰?

- **脏读**：一个事务读到了另一?*未提交事?*修改过的数据。如果那个事务回滚，则读到的数据就是无效?脏数??

- **不可重复?*：在同一个事务内?*两次相同的查?*读到?*不同的数据行内容**。这通常是因为在两次读取之间，另一个事?*修改并提?*了这些数据?

- **幻读**：在同一个事务内?*两次相同的范围查?*读到?*不同的数据行集合**（即出现了新?幻影"行或原有行消失）。这通常是因为在两次查询之间，另一个事?*插入或删?*了符合查询条件的数据行并提交?

它们与隔离级别的对应关系是：

- **读未提交**（Read Uncommitted）级别下?*所有问?*都可能发生?
- **读已提交**（Read Committed）级别下，解决了**脏读**?
- **可重复读**（Repeatable Read，MySQL InnoDB 引擎默认级别）下，解决了**脏读**?*不可重复?*，并通过**Next-Key Lock ?*很大程度上避免了**幻读**?
- **串行?*（Serializable）级别下，通过强制事务串行执行，解决了所有问题，但性能代价最高?

## 深度解析

### 原理与场景化解释

这三种现象的本质区别在于其他事务进行的是**"?**操作还是**"增删"**操作，以及本事务读取时，其他事务是否已提交?

#### 1. 脏读

- **场景**：事?A 将一条记录的余额?100 修改?200?*未提?*）。此时事?B 读取这条记录，得到余?200。随后事?A 因故**回滚**，余额恢复为 100。那么事?B 读到?200 就是从未真实存在过的"脏数??

- **代码示例（伪时序?*?

```sql
事务A：BEGIN; -- ?
事务A：UPDATE account SET balance = 200 WHERE id = 1; -- ?未提交！
事务B：BEGIN; -- ?
        -- 在读未提交级别下，可能读?00这个中间状?
事务B：SELECT balance FROM account WHERE id = 1; -- ?读到?00（脏读）
事务A：ROLLBACK; -- ?数据回滚?00
```

#### 2. 不可重复?vs 幻读

- **核心区别**?*不可重复?*针对的是**已存在数据行的值被修改**?*幻读**针对的是**数据行集合的数量因插入或删除而发生变?*?

- **场景对比**?
    - **不可重复?*：事?A 第一次查?id=1 的余额为 100。此时事?B 将余额更新为 150 ?*提交**。事?A 再次查询 id=1 的余额，得到?150?*同一行，值变?*?
    - **幻读**：事?A 第一次查?`age > 20` 的用户，得到 10 条记录。此时事?B 插入了一?age=25 的新用户?*提交**。事?A 再次以相同条件查询，得到?11 条记录?*多出了一?幻影"**。即使事?B 是删除了 1 条记录，导致事务 A 第二次查询只得到 9 条记录，这也属于幻读?

### MySQL InnoDB 的解决方案与最佳实?

- **可重复读与幻?*：这是最易混淆的点。MySQL InnoDB ?**`REPEATABLE READ`** 级别下，通过 **`MVCC`** 解决?*快照?*（普?`SELECT` 语句）的不可重复读和幻读问题。事务第一次读取时会建立一?*一致性读视图**，后续读取都基于这个视图，看不到其他事务提交的修改?

- **但是**，对?*当前?*（如 `SELECT ... FOR UPDATE`、`UPDATE`、`DELETE` 语句），`REPEATABLE READ` 级别?InnoDB 使用 **`Next-Key Lock`**（记录锁 + 间隙锁）来锁住记录及其附近的间隙，从而阻止其他事务在间隙中插入数据，**很大程度上避免了幻读**。这?MySQL 相较于标?SQL 的增强?

- **最佳实?*?
    1. 理解默认?`REPEATABLE READ` 级别在大多数场景下已足够安全，无需盲目提升?`SERIALIZABLE`?
    2. 在需要绝对防止幻读的**关键业务**（如账户资金变动、唯一性校验），应在事务中使用**显式加锁**（如 `SELECT ... FOR UPDATE`），并确保所有相关查询都走索引，以便 Next-Key Lock 能精确锁定范围，避免锁表?
    3. 在设计阶段，评估业务对一致性的要求。例如，一个后台统计报表可能允许不可重复读（使?`READ COMMITTED` 以获得更高并发），而一个转账操作则必须保证可重复读甚至更严格?

### 常见误区

- **误区一**：认?可重复读"级别完全解决了幻读。实际上，它解决了快照读的幻读，但对于当前读，是通过 Next-Key Lock **预防**，而非从原理上根除。在某些极端复杂的交互序列下，仍然可能观察到类似幻读的现象（但极少见）?

- **误区?*：将"不可重复??幻读"混为一谈。记住关键：**"值变"是不可重复读?行数?是幻?*?

## 总结

简单来说，**脏读**是读了未提交?垃圾"数据?*不可重复?*是两次读同一行数据内容变了，?*幻读**是两次读同一条件数据集合的行数变了。理解它们的区别和成因，是正确选用数据库隔离级别、设计高并发下数据安全访问方案的基础?

---
> 参考来源：[MySQL 脏读、幻读、不可重复读是什么？](https://www.quanxiaoha.com/java-interview/mysql-dirty-read-phantom-read-non-repeatable-read)
